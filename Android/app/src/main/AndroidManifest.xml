<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android">
        <activity android:name=".CancerDetectActivity"></activity>
        <activity android:name=".PeriodTrackerActivity" />
        <activity android:name=".CallsActivity" />
        <activity android:name=".ChatListActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".NewDoctorSearchActivity" />
        <activity
            android:name=".PatientHomeActivity"
            android:label="@string/title_activity_patient_home"
            android:theme="@style/Theme.Android.NoActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>